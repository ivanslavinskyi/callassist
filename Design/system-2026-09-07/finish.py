from pathlib import Path
R=Path(__file__).resolve().parent
note='Десять EN/DE-вариантов публичных документов извлечены из снимков работающего приложения. Немецкие Privacy и Terms восстановлены из `apps/api/src/content/seed-content.ts` (seed revision 1): исходные снимки были пустыми, а при повторной проверке сервер приложения уже не работал. Их актуальные опубликованные ревизии необходимо сверить перед переносом; точное происхождение отмечено в `content-provenance.json`.'
p=R/'GUIDELINES.md';s=p.read_text(encoding='utf-8')
s=s.replace('Тексты опубликованных публичных документов EN/DE извлечены из текущих страниц; этот документ не меняет их юридический смысл.',note)
s=s.replace('Внутренний размер интерактивной зоны принят 44 px и поля 48 px.', 'Внутренний размер интерактивной зоны принят 44 px; стандартные поля — 48 px, компактные desktop-поля основной формы — 44 px.')
s=s.replace('## 9. Состояния основных сценариев','В основной форме на desktop применяются поля 44 px, padding секций 12 px и gap 12 px; счётчик символов размещён внутри нижнего правого угла textarea высотой 112 px. На mobile поля остаются 48 px. Это сохраняет утверждённую плотность формы и видимость Call options при viewport 1300×960.\n\n## 9. Состояния основных сценариев')
p.write_text(s,encoding='utf-8')
p=R/'PAGE-SPECS.md';s=p.read_text(encoding='utf-8').replace('Опубликованные EN/DE тексты сохранены в `content.js`; дата документов — 2 сентября 2026, номера версий сохранены в содержимом.', 'Тексты сохранены в `content.js`; дата документов — 2 сентября 2026. '+note)
p.write_text(s,encoding='utf-8')
p=R/'prepare.py';s=p.read_text(encoding='utf-8');s=s.replace("(ROOT/'content.js').write_text", "existing_path=ROOT/'content.js'\nif existing_path.exists():\n  existing=json.loads(existing_path.read_text(encoding='utf-8').replace('window.PUBLIC_CONTENT=','').rstrip(';\\n'))\n  for key in docs:\n    for lang in docs[key]:\n      if not any(n['tag']=='h1' for n in docs[key][lang]): docs[key][lang]=existing.get(key,{}).get(lang,[])\n(ROOT/'content.js').write_text")
p.write_text(s,encoding='utf-8')
(R/'README.md').write_text('''# SHPROHLI — дизайн-комплект для согласования

7 сентября 2026 · Emerald Paper · светлая и тёмная темы.

Откройте `index.html` для всех макетов. Навигация слева выбирает экран; сверху находятся состояние, тема, ширина и язык. Ссылка «Открыть отдельно» убирает панель атласа. Полная страница прокручивается внутри превью. Все новые страницы ждут дизайн-ревью; ранее утверждена визуальная основа dashboard.

- `boards.html` — галерея 128 browser PNG: 32 экрана × 2 устройства × 2 темы; три обзорные ImageGen-композиции.
- `guidelines.html` / `GUIDELINES.md` — шрифты, цвета, типографика, размеры, сетки, компоненты, состояния, доступность, локализация.
- `page-specs.html` / `PAGE-SPECS.md` — поля, порядок, действия, ограничения всех существующих страниц, включая админку.
- `coverage.html` / `manifest.json` — 30 маршрутных/контентных экранов + 2 системных листа, 161 просмотрочный вариант, связь с компонентами репозитория.
- `tokens.css`, `tokens.json`, `assets/` — значения и исходные ресурсы для будущего переноса.
- `qa.html` / `QA.md`, `design-qa.md`, `qa-*.json`, `contrast.json` — выполненные проверки и их границы.

Для локального просмотра из корня репозитория:

```powershell
python -m http.server 4173 --bind 127.0.0.1 --directory Design/system-2026-09-07
```

Откройте http://127.0.0.1:4173/index.html. Статические HTML также доступны с диска; некоторые браузеры ограничивают переходы между локальными iframe, поэтому локальный сервер предпочтителен. Внешние шрифты/CDN не требуются.

Атлас использует синтетические данные и демонстрационные переходы. Экспорт, воспроизведение, публикация, звонки, SMS и операции аккаунтов не подключены к сервису. Смена формы/темы сбрасывает пример к выбранному состоянию; это каталог макетов, а не модель постоянного хранения данных. Некоторые второстепенные кнопки показывают пояснение вместо реального действия.

Рабочая папка `sources/` содержит свидетельства исходного приложения, включая данные локальной учётной записи. Она исключена из переносимого ZIP и публичной галереи. Два немецких документа используют seed-источник; см. `content-provenance.json`.

Production-файлы приложения не изменены. Перенос начинается отдельным этапом после согласования новых страниц.
''',encoding='utf-8')
print('Updated provenance, compact form specifications and handoff README.')
