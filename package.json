{
  "name": "callassist",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=22.19"
  },
  "scripts": {
    "build": "turbo build",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f postgres",
    "db:migrate": "corepack pnpm --filter @callassist/api db:migrate",
    "db:migrate:check": "corepack pnpm --filter @callassist/api db:migrate:check",
    "db:test:prepare": "corepack pnpm --filter @callassist/api db:test:prepare",
    "db:up": "docker compose up -d --wait postgres",
    "dev": "turbo dev",
    "env:init": "node scripts/init-env.mjs",
    "lint": "turbo lint",
    "security:audit": "corepack pnpm audit --prod --audit-level high",
    "test": "turbo test",
    "tunnel:twilio": "node scripts/start-twilio-tunnel.mjs",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  },
  "pnpm": {
    "overrides": {
      "postcss": "8.5.23",
      "sharp": "0.35.0"
    }
  }
}
